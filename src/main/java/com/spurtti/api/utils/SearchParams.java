package com.spurtti.api.utils;

/**
 * All available search parameter keys. Whenever a new key is added or an existing one is changed, it should be always
 * done here, so that all locations using that key get updated accordingly.
 * 
 * @author Jaakko-H
 *
 */
public class SearchParams {

	public static final String SPORT_TYPE = "sportType";
	
	private SearchParams() {}
}
